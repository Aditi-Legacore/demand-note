The top level pipeline code - i.e. the orchestrator code is kept here. It includes all other modules in the sub-directories and calls them.

Satya to add the top main program here, along with config files or any other top level files.
